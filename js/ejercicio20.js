// La siguiente función devuelve undefined en lugar de la multiplicación, se pide arreglarla:

const a = 3;
const b = 6;
function multiplicar(a, b) {
    let multiplicar1 = a * b;
    console.log(`El resultado es: ${multiplicar1}`);
}
multiplicar(a, b);